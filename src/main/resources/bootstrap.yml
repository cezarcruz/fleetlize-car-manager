info:
  name: Car Manager
  description: Manager car fleet
  version: ${version}


spring:
  application:
    name: car-manager
  cloud:
    consul:
      config:
        enabled: ${CONSUL_ENABLED:false}
